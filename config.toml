[debate]
max_iterations = 4        # max debate rounds before forced synthesis
roles_mode = "default"    # "default" | "archetypes" | "free" | "models"

[model]
orchestrator = "gemini-2.5-flash-lite"
max_tokens   = 4096
# api_base_url = "https://bothub.chat/api/v2/openai/v1"  # default, override if needed

[experts]
expert_1 = "gpt-4.1-nano"
expert_2 = "claude-haiku-4.5"
expert_3 = "gemini-2.5-flash-lite"

[display]
stream     = true
show_usage = false
